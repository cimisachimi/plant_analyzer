// Filename: middleware.ts

export { auth as middleware } from "@/auth"

// (Opsional) Konfigurasi untuk menentukan rute mana yang dilindungi
// export const config = {
//   matcher: ["/dashboard/:path*"],
// }